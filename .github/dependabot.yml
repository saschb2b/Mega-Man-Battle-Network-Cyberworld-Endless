# Keeps the workflow actions and the build images' base images current.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule: { interval: monthly }
    groups:
      actions: { patterns: ["*"] }
  - package-ecosystem: docker
    directory: /docker
    schedule: { interval: monthly }
